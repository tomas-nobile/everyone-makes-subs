---
description: Show the next story and how much time is left
---

1. Read `docs/README.md`: the build order and the checkboxes.
2. Run `date` and compute the time left until 11:45 ART on Sept 25 (submission margin).
3. Reply in 5 lines max:
   - Next pending story in order (and the other lane's, if relevant).
   - Hours left against the plan in `docs/README.md`.
   - If we're behind: what to cut, following "Cut order".
